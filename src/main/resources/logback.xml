<configuration>

    <!--<statusListener class="ch.qos.logback.core.status.OnConsoleStatusListener" />--> <!--Scan this configuration for errors-->

    <!-- Properties -->
    <property name="LOG_DIR" value="logs" />
    <property name="MAX_HISTORY" value="7" />
    <property name="TOTAL_SIZE" value="100MB" />
    <property name="SMTP_HOST" value="smtp.example.com" />
    <property name="SMTP_PORT" value="587" />
    <property name="SMTP_USER" value="your-email@example.com" />
    <property name="SMTP_PASS" value="your-password" />
    <property name="EMAIL_TO" value="admin@example.com" />
    <property name="EMAIL_FROM" value="noreply@example.com" />
    <property name="EMAIL_SUBJECT" value="Critical Error in Application" />

    <!-- Console appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] [%-5level] %logger{36} [%X{contextInfo}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender -->
    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/application-%d{dd-MM-yyyy}-%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE}</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] [%-5level] %logger{36} [%X{contextInfo}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- async appender -->
    <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="ROLLING_FILE" />
    </appender>


    <!--Appernder only for the error logs-->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/error-%d{dd-MM-yyyy}.log.gz</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] [%-5level] %logger{36} [%X{contextInfo}] - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!--Email appender -->
    <appender name="EMAIL" class="ch.qos.logback.classic.net.SMTPAppender">
        <smtpHost>smtp.example.com</smtpHost>
        <smtpPort>587</smtpPort>
        <username>your-email@example.com</username>
        <password>your-password</password>
        <to>admin@example.com</to>
        <from>noreply@example.com</from>
        <subject>Critical Error in Application</subject>
        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] [%-5level] %logger{36} [%X{contextInfo}] - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
    </appender>

    <!-- Root logger -->
    <root level="debug">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="ASYNC" />
        <appender-ref ref="ERROR_FILE" />
        <!--<appender-ref ref="EMAIL" />-->
    </root>
</configuration>
